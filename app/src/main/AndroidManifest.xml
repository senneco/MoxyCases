<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="net.senneco.moxy.cases">

	<!-- To access Google+ APIs: -->
	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".LauncherActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".case1.MainActivity" />
		<activity android:name=".case2.MainActivity" />
		<activity android:name=".case3.MainActivity" />
		<activity android:name=".case4.MainActivity" />
		<activity android:name=".case5.MainActivity" />
		<activity android:name=".case6.MainActivity" />
		<activity android:name=".case7.MainActivity" />
		<activity android:name=".case8.DetailsActivity" />
		<activity android:name=".case9.DetailsActivity" />
	</application>

</manifest>